const THRESHOLD = 0.6
const MIN_NODE_VERSION = '16.10.0'
const DEFAULT_PORT = '8080'
const DEFAULT_CONFIG_FILE = ['chun.config.(js|json|mjs)']

module.exports = {
  THRESHOLD,
  MIN_NODE_VERSION,
  DEFAULT_PORT,
  DEFAULT_CONFIG_FILE
}
